resolution: workspace
name: shared
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  mason: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter