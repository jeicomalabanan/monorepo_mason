name: flutter_package
description: Generate a flutter package.
version: 1.0.0

environment:
  mason: ^0.1.2

vars:
  package_name:
    type: string
    description: The name of the flutter package.
    prompt: What is package name?
