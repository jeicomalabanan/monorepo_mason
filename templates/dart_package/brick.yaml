name: dart_package
description: Generate a dart package.
version: 1.0.0

environment:
  mason: ^0.1.2

vars:
  package_name:
    type: string
    description: The name of the dart package.
    prompt: What is package name?
