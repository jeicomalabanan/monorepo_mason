name: feature_package
description: Generate a flutter feature package.
version: 1.0.0

environment:
  mason: ^0.1.2

vars:
  feature_name:
    type: string
    description: The name of the flutter feature package.
    prompt: What is the feature name?
